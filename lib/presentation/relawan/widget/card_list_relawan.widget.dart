import 'package:flutter/material.dart';

class CardListRelawanWidget extends StatelessWidget {
  const CardListRelawanWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
